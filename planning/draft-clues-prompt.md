You are drafting candidate trivia clues for the trivia_trainer app, for a HUMAN to review before import. You are running locally and unattended (a scheduled task), with full local access: Wikipedia is reachable, Supabase credentials are in .env.local, git push works, and the `gh` CLI is authenticated.

HARD RULES: Draft only. Do NOT import live into Supabase (dry-run only). Do NOT merge any PR.

EACH RUN, draft for TWO different under-covered categories, delivered in a SINGLE pull request. Complete steps 2–6 for the first category, then repeat steps 2–6 for a second, different under-covered category. Then do step 7 ONCE to commit both categories on one branch and open ONE combined PR. Summarize both in step 8, then stop.

Steps:

1. Read planning/clue-authoring-guide.md (the calibration rules) and data/sourcing/subcategories.json (valid subcategory names per category). Internalize: declarative "this/it" phrasing, not interrogative; NO answer leaks — the clue must not contain the answer's words or any word sharing a root/stem with the answer (e.g. "southernmost" leaks "Southern Ocean"); difficulty comes from revealing LESS and asking from the surprising end, NOT from stacking facts ($200-400 = direct recall, $600 = triangulation, $800-1000 = a single oblique/counterintuitive hook). Write each clue ONLY from a fact stated in a cited source doc — never from memory. Also read data/sourcing/category-guide.json — per category it lists the subcategories and seed topics worth covering. **And read planning/edit-lessons.md** — distilled patterns from how humans have edited past drafts (difficulty calibration, categorize-by-subject, clue craft, leaks/aliases, wordplay style). Pre-apply those lessons so reviewers stop having to make the same fixes.

2. Pick ONE under-covered category (a different one each time through steps 2–6 — never the category you already drafted earlier this run). Query Supabase for active clue counts by category (use tools/acquisition/acquisition-utils.mjs with the .env.local credentials, the same pattern verify-clue-sources.mjs uses with --from-db). SKIP geography (already at 200). **SKIP language_wordplay** — real wordplay (anagrams, Before & After, puns, homophones) must be CONSTRUCTED, not sourced from a Wikipedia extract; trying to source it just yields mis-categorized vocabulary/definition clues. It's handled by a separate generator, not this drafter. Also skip any category that already has a recent draft in data/sourcing/packs/drafts/ unless every non-geography category has one. **Categorize by SUBJECT, not by linguistic angle** — a "umami" clue is Food & Drink (pop_culture_media_modern_life), a medical term is science / Medicine & Anatomy, a card game is sports_games_leisure / Board Games & Card Games.

3. BUILD THE CATEGORY'S OWN SOURCE DOCS (mandatory every run). Run `node tools/acquisition/build-topic-docs.mjs --category <category> --limit 25`. This fetches cited Wikipedia docs from that category's topic store into data/sourcing/docs/<category>/ (idempotent — it skips docs that already exist, so it's cheap when the corpus is already grown). Wikipedia is reachable here. Do this BEFORE authoring so the clues are grounded in the chosen category's OWN docs. NEVER borrow another category's docs (e.g. do not write History clues from geography docs); if after this step the category still has too few usable docs to write ~15 good clues, pick a different under-covered category and build its docs instead.

4. Author ~15 clues grounded ONLY in the Wikipedia lead extracts in data/sourcing/docs/<category>/ (the folder for your chosen category). Attach each source doc's {source,title,url} citation to its clue. Give each clue a category_id and a subcategory_name that EXACTLY matches an entry in data/sourcing/subcategories.json for that category. Match the exact pack shape used by existing files in data/sourcing/packs/ — open one and copy its field names. Quality over volume; ~15 is plenty. **Every clue in a run must have a DISTINCT answer** — never write two clues for the same answer or work (e.g. two "Beethoven's Fifth Symphony" clues), even across the run's two categories; it makes the set repetitive. (It's perfectly fine to draw several DIFFERENT answers from the same source doc.) The import dry-run in step 6 now **hard-fails on any duplicate answer**, so a pack with repeats cannot pass the gate — dedupe before opening the PR. Spread the clues across several of the category's subcategories (per category-guide.json) instead of clustering in one, and favor subcategories that look thin in the active bank; use the guide's seed topics to decide what to look for and which docs to prioritize in step 3.
   VISUAL CLUES — when the category is arts_visual_culture, include at least 3 image clues alongside the text clues. For each: pick a famous PUBLIC-DOMAIN painting (the artist must have died ~75+ years ago — e.g. da Vinci, Vermeer, Van Gogh, Rembrandt, Hokusai, Monet; NEVER modern/copyrighted art like Picasso, Pollock, Warhol, Dalí, Kahlo). Get its image from the painting's Wikipedia REST summary (`originalimage.source`, else `thumbnail.source`) — the Wikimedia Commons URL. Set `mechanic: "visual"`, `subcategory_name: "Famous Artworks"`, `image_url` (that URL), `image_attribution: null` (the app shows no on-screen credit — public-domain works need none, and any artist text there would leak the answer on "Name the artist" clues), `image_license: "public-domain"`. **Randomly alternate the prompt**: about half should be `clue: "Name this painting."` with `answer` = the painting's title, and about half `clue: "Name the artist."` with `answer` = the painter. On EVERY visual clue set `answer_detail: "<Title> — <Artist>"` (the full reveal shown on submission, regardless of which was asked), and keep each clue's `aliases` matched to its OWN answer — title aliases for a painting clue, the artist's surname (keep particles, e.g. "van Gogh", "da Vinci") for an artist clue. `tools/acquisition/build-visual-arts-pack.mjs` is a working example you can run or copy.

5. Write the pack to data/sourcing/packs/drafts/<category>-draft-<YYYY-MM-DD>.json (use today's date).

6. Verify: run `node tools/acquisition/verify-clue-sources.mjs --pack <that pack> --write-back`. Then run `node tools/acquisition/import-to-supabase.mjs <that pack> --dry-run` to get the quality + difficulty gate result. Do NOT run a live import.

7. Open ONE pull request covering BOTH categories. Do this only after you have authored and verified both packs (both categories' steps 2–6 are done). First start from a clean, current main so the daily harvest's local changes don't ride along: `git checkout main && git pull --ff-only origin main`, then create a single dated branch `git checkout -b draft-clues/daily-<YYYY-MM-DD>`. Commit **only your own new files, by explicit path** — BOTH draft packs and BOTH categories' docs folders:
   `git add data/sourcing/packs/drafts/<packA>.json data/sourcing/docs/<categoryA>/ data/sourcing/packs/drafts/<packB>.json data/sourcing/docs/<categoryB>/`
   then commit (a single commit is fine). **NEVER run `git add -A` or `git add .`** — derived/runtime files (topic stores, harvest state/logs, regenerated reports) must never be committed, or unreviewed daily PRs will collide on them. Push and run `gh pr create` ONCE, with a body that has a clearly separated section per category (clue count, verify result, dry-run gate, and any flags). Do NOT merge. After the PR is open, run `git checkout main` so the repo is left on a clean `main` (not your draft branch).

8. Print a concise final summary covering BOTH categories: for each, the category chosen and why (with the active-count that made it under-covered), how many new docs step 3 fetched, clue count, the verify result (verified/weak/unverified), the dry-run quality/difficulty gate result, and any leak or quality concerns you caught and how you handled them. End with the single PR URL.

Remember: this is a draft for human review. Never import live, never merge.
